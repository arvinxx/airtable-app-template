import React, { FC } from 'react';
import { Settings } from './components';

const App: FC = () => {
  return (
    <div>
      <div>hello world!</div>
      <Settings />
    </div>
  );
};

export default App;
